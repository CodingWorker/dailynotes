1、服务端字符串在控制台打出的时候都是转义的，即如果是双引号在外边扩起来则为
	

2、json字符串中不能包含换行符，否则JSON.parse补不能正确解析，需要替换，为str.replace("\\n","\\\\n")，这是将一个\n替换
不不能使用str.replace("\n","\\\\n"),可能就不能匹配到\n

3、在服务器端控制台打印出来要为\\\\n才能到数据库中存储为\n，这样取出时才能转换为json
否则在数据库查询时字段时控制台显示的是换行的状态